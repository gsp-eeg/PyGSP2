version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

formats:
  - htmlzip

sphinx:
  builder: html
  configuration: doc/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev
